# Computer Architecture and Hardware

**Phase 1 · Section 2**

Everything is in **[Guide.md](Guide.md)**.

The guide covers:

1. CPU vs GPU — the two computing paradigms and why they exist
2. Instruction Set Architecture — ARM64, x86-64, RISC-V
3. CPU pipeline, out-of-order execution, branch prediction
4. Memory hierarchy — caches, DRAM, HBM, cache coherence (MESI)
5. SIMD → GPU SIMT — the vector execution bridge
6. GPU architecture — SM internals, warp scheduling, Tensor Cores
7. AI accelerator patterns — systolic arrays, dataflow, NPUs
8. Roofline model — arithmetic intensity, memory-bound vs compute-bound
9. Labs — cache profiling, SIMD benchmarking, Nsight Compute, ISA comparison
